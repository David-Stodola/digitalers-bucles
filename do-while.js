///const estacion = prompt("Ingrese una estacionn del ano").toLocaleLowerCase();

///while () {
    
    //Le vuelvo a pedir que haga un prompt 
    ///estacion = prompt("Ingrese una estacion del ano").toLocaleLowerCase;
///}


//const estacion = prompt("Ingrese una estacionn del ano").toLocaleLowerCase();

let edadCliente = parseInt(prompt("Ingrese edad del cliente"));

while (isNaN(edadCliente || edadCliente < 120  <+0 || )) {
    
    //le vuelvo a pedir que haga un prompt
    edadCliente = parseInt(prompt("Ingrese edad de cliente"));
    
}

console.log(edadCliente)